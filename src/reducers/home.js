/**
 * Created by huangchao on 13/11/2017.
 */

const initialState = {
  list: ['sd'],
}

export default (state = initialState, action = {}) => {
  switch (action.type) {
    default:
      return state
  }
}
